{
  "name": "creatix-ai-live",
  "version": "3.1.1",
  "private": true,
  "type": "module",
  "description": "Plateforme PWA de découverte et de lecture intégrée de conférences IA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm test && npm run typecheck && npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.10",
    "@netlify/functions": "^5.1.1",
    "hls.js": "^1.6.13"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  },
  "engines": {
    "node": "24.x"
  }
}
